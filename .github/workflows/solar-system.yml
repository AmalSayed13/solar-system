name: first w.f

on: 
  workflow_dispatch:
  push:
    branches:
      - main
    
jobs:
   first:
        runs-on: ubuntu-latest
        steps:
        - name: print msg
          run: echo "Amal"

        
